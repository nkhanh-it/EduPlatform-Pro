server:
  port: 8080

spring:
  datasource:
    url: "jdbc:postgresql://db.pbdolgaaoqsbkckgudoi.supabase.co:5432/postgres?sslmode=require"
    driver-class-name: org.postgresql.Driver
    username: "postgres"
    password: '2_@LVAW/fqm-kGS'
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
  jackson:
    time-zone: Asia/Ho_Chi_Minh

app:
  jwt:
    secret: "CHANGE_ME_TO_A_LONG_RANDOM_SECRET"
    expiration-minutes: 120
    refresh-expiration-days: 30
  payment:
    frontend-return-url: ${APP_PAYMENT_VNPAY_FRONTEND_RETURN_URL:http://localhost:3000/checkout}
    vnpay:
      enabled: ${APP_PAYMENT_VNPAY_ENABLED:false}
      tmn-code: ${APP_PAYMENT_VNPAY_TMN_CODE:}
      hash-secret: ${APP_PAYMENT_VNPAY_HASH_SECRET:}
      payment-url: ${APP_PAYMENT_VNPAY_PAYMENT_URL:https://sandbox.vnpayment.vn/paymentv2/vpcpay.html}
      return-url: ${APP_PAYMENT_VNPAY_RETURN_URL:http://localhost:8080/api/payments/vnpay/return}

logging:
  level:
    org.springframework.security: INFO
